'''
    Ejemplo 6 usaremos algo que se utilizaba en el lenguaje C.
    El operador ternario se trata de que en una sola línea de código
    van incluidas tanto el if y el else.
    Pero en este caso lo utilizaremos en una salida print() todo irá
    dentro del print().
'''
z = 2
# La sintaxis es la siguiente:
# "Mostra si cumple"  if condicion else " No se cumple"  
print("Es Mayor" if z > 4 else "No lo es")